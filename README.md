# purescript-erl-test-eunit
The thinnest of EUnit test wrappers

Use Erlang's EUnit as a test runner, and your choice of assertion library (i.e. `purescript-assert`).

For usage examples, see `test/`.
